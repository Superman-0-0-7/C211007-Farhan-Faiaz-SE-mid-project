  function redirectTo(option) {
    if (option === 'todo') {
      window.location.href = 'todo.html';  // Redirect to the To-Do List page
    } else if (option === 'bmi') {
      window.location.href = 'bmi.html';  // Redirect to your BMI Calculator page
    } else if (option === 'binary') {
      window.location.href = 'binary.html';  // Redirect to your Binary to Decimal Converter page
    }
  }
  



  function redirectTo(option) {
    if (option === 'todo') {
      window.location.href = 'todo.html';
    } else if (option === 'bmi') {
      window.location.href = 'bmi.html'; // Redirect to the BMI Calculator page
    } else if (option === 'binary') {
      window.location.href = 'binary.html';
    }
  }
  


  function redirectTo(option) {
    if (option === 'todo') {
      window.location.href = 'todo.html';
    } else if (option === 'bmi') {
      window.location.href = 'bmi.html';
    } else if (option === 'binary') {
      window.location.href = 'binary.html'; // Redirect to the Binary to Decimal Converter page
    }
  }
  

